# Application name
spring.application.name=farm_api

# Database connection
spring.datasource.url=jdbc:mysql://localhost:3306/farm_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=0BooT0@f45
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate settings
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
# The following line is no longer needed as Hibernate auto-detects the dialect.
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Optional: Disable open-in-view to avoid a warning in the logs
spring.jpa.open-in-view=false